# 云台控制协议分析报告

## 概述

本文档详细分析了机器人车项目中云台控制系统使用的通信协议。通过对代码库的深入研究，发现云台控制采用**基于JSON格式的串行通信协议**。

## 协议类型

### 通信方式
- **协议类型**: JSON over Serial/UART
- **通信接口**: 串行通信 (Serial/UART)
- **设备接口**: 
  - 树莓派5: `/dev/ttyAMA0`
  - 其他树莓派: `/dev/serial0`
- **波特率**: 115200 bps
- **数据格式**: JSON字符串，以换行符('\n')结尾

### 命令队列机制
- 采用线程安全的命令队列系统
- 所有命令通过 `BaseController.send_command()` 方法加入队列
- 专用线程 `process_commands()` 负责串行发送命令

## 云台控制命令格式

### 主要控制命令 (T: 133)
```json
{
  "T": 133,           // 命令类型 (cmd_gimbal_ctrl)
  "X": <pan_angle>,   // 水平角度 (-180 ~ 180度)
  "Y": <tilt_angle>,  // 垂直角度 (-30 ~ 90度)  
  "SPD": <speed>,     // 运动速度
  "ACC": <acceleration> // 加速度值
}
```

### 基础控制命令 (T: 141)
```json
{
  "T": 141,           // 命令类型 (cmd_gimbal_base_ctrl)
  "X": <pan_angle>,   // 水平角度
  "Y": <tilt_angle>,  // 垂直角度
  "SPD": <speed>      // 运动速度
}
```

### 稳定模式命令 (T: 137)
```json
{
  "T": 137,           // 命令类型 (cmd_gimbal_steady)
  "s": <mode>,        // 稳定模式 (0: 关闭, 1: 开启)
  "y": <bias>         // Y轴偏置值
}
```

## 控制参数详解

### 角度范围
- **水平轴 (Pan/X轴)**:
  - 范围: -180° 到 +180°
  - 负值: 向左转
  - 正值: 向右转
  
- **垂直轴 (Tilt/Y轴)**:
  - 范围: -30° 到 +90°
  - 负值: 向下倾斜
  - 正值: 向上倾斜

### 速度和加速度
- **SPD (速度)**: 控制云台运动的速度，数值越大运动越快
- **ACC (加速度)**: 控制云台运动的加速度，影响运动的平滑度

## 配置参数

### 配置文件位置
- 主配置: `robot_body/config/config.yaml`

### 相关配置项
```yaml
cmd_config:
  cmd_gimbal_ctrl: 133      # 主要云台控制命令
  cmd_gimbal_base_ctrl: 141 # 基础云台控制命令  
  cmd_gimbal_steady: 137    # 云台稳定模式命令

cv:
  track_spd_rate: 60        # 跟踪速度率
  track_acc_rate: 0.4       # 跟踪加速度率
  track_faces_iterate: 0.045 # 人脸跟踪迭代率
  track_color_iterate: 0.023 # 颜色跟踪迭代率
```

## 控制接口

### 1. 程序接口
```python
# 直接控制
base.gimbal_ctrl(x_angle, y_angle, speed, acceleration)
base.gimbal_base_ctrl(x_angle, y_angle, speed)

# JSON命令控制
base.base_json_ctrl({"T":133, "X":0, "Y":0, "SPD":10, "ACC":128})
```

### 2. Web界面控制
- 通过虚拟摇杆控制云台运动
- 支持实时角度显示和可视化反馈
- 游戏手柄/控制器支持

### 3. 计算机视觉集成
- 人脸跟踪自动控制
- 颜色目标跟踪
- 手势识别控制

## 技术特点

### 优势
1. **简单易懂**: JSON格式便于调试和扩展
2. **实时性好**: 串行通信延迟低，响应快速
3. **集成度高**: 与视觉系统、Web界面良好集成
4. **稳定可靠**: 队列机制确保命令有序执行

### 架构设计
1. **分层设计**: 硬件驱动层、控制逻辑层、应用接口层
2. **模块化**: 云台控制独立封装，便于维护
3. **多输入源**: 支持程序控制、Web界面、CV系统等多种输入

## 使用示例

### 基本云台控制
```python
# 云台回到中心位置
base.gimbal_ctrl(0, 0, 200, 128)

# 向右转45度，向上仰30度
base.gimbal_ctrl(45, 30, 100, 64)

# 快速左转
base.gimbal_ctrl(-90, 0, 500, 200)
```

### 视觉跟踪应用
```python
# CV跟踪中的云台控制 (来自cv_ctrl.py)
def gimbal_track(self, fx, fy, gx, gy, iterate):
    distance = math.sqrt((fx - gx) ** 2 + (gy - fy) ** 2)
    self.pan_angle += (gx - fx) * iterate
    self.tilt_angle += (fy - gy) * iterate
    
    # 角度限制
    self.pan_angle = max(-180, min(self.pan_angle, 180))
    self.tilt_angle = max(-30, min(self.tilt_angle, 90))
    
    # 发送控制命令
    gimbal_spd = int(distance * self.track_spd_rate)
    gimbal_acc = int(distance * self.track_acc_rate)
    self.base_ctrl.base_json_ctrl({
        "T": self.CMD_GIMBAL,
        "X": self.pan_angle,
        "Y": self.tilt_angle,
        "SPD": gimbal_spd,
        "ACC": gimbal_acc
    })
```

## 结论

云台控制系统采用**JSON over Serial协议**，具有以下特点：
- 通信简单高效，基于标准串行接口
- 命令格式清晰，便于理解和调试  
- 集成度高，支持多种控制方式
- 实时性能好，适合机器人控制应用

该协议设计合理，既保证了控制的实时性和准确性，又具有良好的可扩展性和维护性。